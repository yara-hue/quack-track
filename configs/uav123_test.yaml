data:
  coco_root: null
  coco_samples: 50000
  pairs_per_seq: 10
  search_sz: 255
  template_sz: 127
  uav123_root: C:/Users/ehabn/quack-track/data/UAV123_10fps
  uav20l_root: ./data/UAV20L
eval:
  device: cuda
  search_sz: 255
  template_sz: 127
loss:
  cls_weight: 1.0
  reg_weight: 1.2
model:
  backbone:
    frozen: true
    pretrained: true
  fpn:
    multi_level_train: true
    out_channels: 256
  head:
    num_anchors: 5
train:
  batch_size: 8
  epochs: 5
  lr: 0.001
  momentum: 0.9
  num_workers: 0
  save_every: 5
  warmup_epochs: 1
  weight_decay: 0.0001
